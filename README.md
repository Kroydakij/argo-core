# argo-core
Fondamenta per suite gestionali **Flask + SQLite** che girano su un PC qualunque: Windows senza diritti admin, niente Docker, niente server, niente cloud. Serving HTTP in LAN, backup = copia di una cartella.
